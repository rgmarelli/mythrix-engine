# Symbol Interpretation Core — Tasks

Ordered breakdown of `plan.md`. Each task is independently verifiable.

- [x] T1: Rewrite `spec.md` to the Sign/Manifestation/Property/Interpretant/IntersemioticInterpretant vocabulary (FR1-3, FR8, FR9-13, FR16-24, FR27-29, worked example, reference implementation scope).
- [x] T2: Write `plan.md` (domain model, Kùzu schema, graph store, authoring YAML/loader, retrieval pipeline, data migration, local DB recreation, risks).
- [x] T3: Rewrite `core/models.py`: `Property`, `QueryDirective`, `Interpretant`, `Sign`, `IntersemioticInterpretant`, `Manifestation`, `SignSummary`, `GraphFacts`; retrieval-result types (`RetrievedPassage`, `ConceptCandidates`, `ConceptMatchScore`, `MergedCandidate`, `ConceptPairCandidates`, `RetrievalContext`) keep their names with `GraphFacts.sign`/`.manifestation` field renames.
- [x] T4: Rewrite `core/graph/schema.py` DDL: `Sign`, `Tradition`, `Manifestation`, `Property`, `Interpretant`, `Source` node tables; `HAS_MANIFESTATION`, `MANIFESTED_IN`, `HAS_PROPERTY`, `HAS_INTERPRETANT`, `CITES`, `INTERSEMIOTIC` rel tables.
- [x] T5: Rewrite `core/graph/store.py`: `upsert_sign`, `upsert_sign_with_manifestation`, `upsert_intersemiotic_interpretant`, `reconcile_sign_manifestations`, `get_manifestation`, `list_signs`, and the renamed internal lookups (`_get_sign_properties`, `_get_manifestation_properties`, `_get_interpretants`, `_get_all_manifestation_interpretants`, `_get_sign_intersemiotic_interpretants`).
- [x] T6: Replace `core/loaders/symbol_schema.py`/`symbol_loader.py` with `sign_schema.py`/`sign_loader.py`: `SignFile`, `SignBlock`, `PropertyEntry`, `QueryDirectiveEntry`, `InterpretantEntry`, `IntersemioticInterpretantEntry`, `ManifestationEntry`; `target_system`-scoped resolution; drop `retrievable`; fold keywords into interpretants.
- [x] T7: Rewrite `core/retrieval/pipeline.py`: replace `_NUMBER_WORDS`/`_number_for`/`_is_retrievable` with `interpretant.query.directive == "filter"` / `.as_token` reads; rename `_Number`→`_FilterToken`, `_collect_numbers`→`_collect_filter_tokens`; ensure `properties` (sign- or manifestation-level) are excluded from query-text construction everywhere, including inside an intersemiotic interpretant's target; keep RRF/concept-scoping/pair-convergence/boost-not-replace/global-not-per-group behavior unchanged; rewrite the module docstring's number-handling paragraphs.
- [x] T8: Fix mechanical downstream references in `core/query_service.py`, `api/routes.py`, `cli/formatting.py`, `core/synthesis/prompts.py`, `cli/commands/{query,load_symbols}.py` for renamed fields/methods — no behavior change.
- [x] T9: Write `scripts/migrate_to_sign_model.py`, run it against `data/**/*.yaml` and `tests/fixtures/{tarot,kabbalah}/**/*.yaml`, hand-review every migrated file's diff, then delete the script.
- [x] T10: Update tests: `test_models.py`, `test_graph_schema.py`, `test_graph_store.py`, `test_kuzu_multi_edge_risk.py` (re-target at `HAS_PROPERTY`), `test_symbol_schema.py`→`test_sign_schema.py`, `test_symbol_loader.py`→`test_sign_loader.py` (add `target_system`-scoping and error-path cases), `test_retrieval_pipeline.py` (rewrite fixtures; replace `_NUMBER_WORDS` assertions with `query.directive` assertions; add a regression test proving a relationship target's properties never appear in built query text), `test_prompts.py`, `test_formatting.py`, `test_query_service.py`, `test_api.py`, `test_cli_query.py`, `test_citations.py`, `test_domain_agnosticism.py`.
- [x] T11: Delete `.mythrix/graph.kuzu`; re-run `mythrix load-symbols`/`mythrix load-documents` against migrated `data/`; run `mythrix query` for "The Sun"/`rider-waite` and confirm the Qoph/laughter convergence and the `numeric_value`→`"hundred"` filter still surface the Genesis 21/17 passages.
- [x] T12: `ruff check .`, `ruff format --check .`, full `pytest` suite, and the domain-agnosticism guardrail test all pass.
- [x] T13: Drop the dead `value_type` field from `Property`/`Interpretant` (schema, store, loader, all layers) since nothing reads it; nest `manifestations` under `sign:` in the authoring format (`SignBlock.manifestations` instead of a top-level `SignFile.manifestations`), re-migrate all 49 data/fixture files accordingly, update tests, re-verify end-to-end.
- [ ] T14: Add `domain`/`citation_label`/`description` and an authored `id` to `Source` (model, DDL, store, `SourceBlock`); drop `Tradition` from the vector/retrieval layer entirely (`ChunkMetadata`/`VectorHit`/`RetrievedPassage` lose `tradition_slug`/`tradition`; `similarity_search` loses its tradition parameter); passage attribution renders `source.citation_label` (falling back to `title, author`).
- [ ] T15: Rewrite `document_loader.load_document()` to derive `domain` from the declared `Source` instead of caller-supplied `tradition_slug`/`domain`; add `load_corpus_directory()` for auto-discovery of colocated `<name>.yaml`/`<name>.txt` corpus source pairs (two-phase: parse+dedup-check every pair, then register+ingest); rewrite `mythrix load-documents` to take a directory root with no `--tradition`/`--domain`/`--source-slug`.
- [ ] T16: Restructure `data/tarot/` → `data/semiotic_systems/tarot/`, `data/kabbalah/` → `data/semiotic_systems/hebrew_alef_bet/` (renaming `semiotic_system: tarot_cards` → `tarot` throughout to match the directory exactly), `data/bible/` → `data/corpus/scripture/en_drb/` (colocated YAML+TXT, `Tradition` dropped); same restructuring for `tests/fixtures/`; backfill `id`/`domain` on the three existing citation sources.
- [ ] T17: Update all affected tests (`test_vector_store.py`, `test_document_loader.py`, `test_cli_load_documents.py`, `test_models.py`, `test_retrieval_pipeline.py`, `test_query_service.py`, `test_cli_query.py`, `test_api.py`, `test_formatting.py`, `test_citations.py`, `test_prompts.py`, `test_graph_store.py`, `test_sign_schema.py`, `test_citation_resolution.py`, `test_cli_main.py`); update `spec.md`/`plan.md` for the corpus/domain architecture; reload the local Kùzu DB against the new `Source` columns and re-verify end-to-end (`ruff check .`, `ruff format --check .`, full `pytest`, a real `mythrix load-symbols`/`load-documents`/`query` cycle with `citation_label` visible in output).
